node_modules/
output/
.spago/
.psa-stash
dist/
public/dist/
# Locally-staged model checkpoints (weights/tokenizer files are user-fetched).
public/local/
*.log
.DS_Store
# Claude / agent metadata. Personal settings stay ignored; project-shared
# rules and skills are explicitly un-ignored below so collaborators get
# them via git.
.claude/*
!.claude/fp-police-rules.md
!.claude/skills
.claude/settings.local.json
